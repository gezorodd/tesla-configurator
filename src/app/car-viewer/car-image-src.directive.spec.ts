import { CarImageSrcDirective } from './car-image-src.directive';

describe('CarImageSrcDirective', () => {
  it('should create an instance', () => {
    const directive = new CarImageSrcDirective();
    expect(directive).toBeTruthy();
  });
});
