import { CarConfigSpecPipe } from './car-config-spec.pipe';

describe('CarConfigSpecPipe', () => {
  it('create an instance', () => {
    const pipe = new CarConfigSpecPipe();
    expect(pipe).toBeTruthy();
  });
});
